### Funcionais

- Sistema de administração interno
    - Controle de usuários, acessos e permissões
    - Cadastros:
        - Clientes
        - Veículos
        - Vagas
        - Entradas e saídas de veículos
    - Status de vagas automático (ocupado / livre)
- API para futuras integrações
    - Cadastros completos das entidades
        - Clientes
            - (User(Usar modelo Users do django(Blank and null True)),name, cpf, phone, created_at, updated_at)
        - Veiculos
            - Veículos(owner(FK(Clientes)(Nullable and Blank)),license_plate, marca do veiculo(FK(Marca_do_veiculo)),tipo do veiculo(FK(tipo_veiculo)))
            - tipo_veiculo( tipo_veiculo, description)
            - Marca_do_veiculo(Marca do veiculo)
        - Vagas
            - Vagas (Vaga, is_occupied, created_at, updated_at)
            - Histórico de vagas (vehicle(FK(Vehicle)), Vaga, entry_time, exit_time)
    - Autenticação
    - Filtros
    - Possibilidade de acesso dos clientes
        - Ver apenas seus veículos e registros
- Auto completar dados dos veículos quando informado apenas a placa
    - Verificar API de consulta
    - Rodar em paralelo
- Notificar proprietário nas entradas e saídas dos veículos
    - WhatsApp
        - EvolutionApi
    - E-mail
        - SMTP

### Não funcionais

- Dashboard do Jazzmin
- API Restfull
- Linter e pep08
- JWT token para API
- RQL para filtros
- Testes
- Serviços em containers
- EvolutionApi (serviço e integração)
- E-mail SMTP (integração e envio)
- PostgreSQL
- Celery e RabbitMQ
    - Rodar tasks de buscas de auto completar dados de veículos
- Documentação da API (swagger)